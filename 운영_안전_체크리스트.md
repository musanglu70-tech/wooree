# 유료 실서비스 안전 운영 체크리스트

> 나중에 유료 전환/실서비스 오픈할 때 할 일. (스택: Next.js + Supabase + Vercel, 처방 데이터)

## 우선순위 TOP 3 (최소 안전선)
1. **백업**: Supabase Pro + 외부 백업 → 데이터 안전
2. **비용 상한**: API·호스팅 사용량 한도 → 요금 폭탄 방지
3. **2FA 전부 켜기**: 계정 안전

---

## 1. 백업 (제일 중요 — 데이터 잃으면 사업 끝)
- [ ] **Supabase Pro($25/월)** 업그레이드 → 자동 백업 + 7일 시점복구(PITR)
- [ ] **주 1회 수동 백업**을 외부(구글드라이브 등)에
      (Supabase Database → Backups 다운로드, 또는 `pg_dump`)

## 2. 비용 폭탄 방지 (유료의 진짜 위험)
- [ ] **Anthropic(OCR/AI) 월 사용량 상한** 설정 (예: $50) — 크론이 매일 도니 누적됨
- [ ] **Vercel / Supabase 사용량 알림·상한** 설정
- 안 하면 어느 달 갑자기 수십만 원 나올 수 있음

## 3. 보안 운영
- [ ] **Supabase / Vercel / GitHub 2FA(2단계 인증)** 전부 켜기 — 제일 싸고 효과 큰 조치
- [x] 비밀키(service_role 등)는 Vercel 환경변수에만 (코드·GitHub 금지) — 이미 됨
- [ ] **오픈 전 RLS 격리 점검 1회** — 처방 데이터라 필수
      (파트너/타테넌트 계정으로 남의 데이터 안 보이는지 침투 테스트)

## 4. 장애 대응
- [ ] **Vercel "즉시 롤백"** 버튼 위치 숙지 (새 배포 문제 시 1클릭 복구)
- [ ] **Sentry(무료)** 에러 추적 연결 → 사용자보다 먼저 문제 인지
- [ ] **UptimeRobot(무료)** 작동 감시 → 앱 죽으면 알림

## 5. 접근 관리
- [ ] 계정 정보·환경변수 목록을 안전한 곳에 문서화
      (대표 혼자만 알면, 못 하는 상황에서 사업 멈춤)

---

## 참고: 현재 시스템 구성 (인수인계용 메모)
- **배포**: Vercel (wooree) ← GitHub main 브랜치 자동 배포
- **DB/인증**: Supabase (프로젝트 oqpsrdlsajyapthbojnf)
- **SQL 마이그레이션 순서**: `scripts/supabase/` 폴더 01 → 11 (순서대로 실행해야 RLS 정책이 올바르게 쌓임)
- **환경변수(Vercel)**: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ANTHROPIC_API_KEY, RESEND_API_KEY, FROM_EMAIL, GMAIL_CLIENT_ID/SECRET/REFRESH_TOKEN, CRON_SECRET, GOOGLE_VISION_API_KEY
- **Supabase 설정**: Authentication → Confirm email = OFF (사업자번호 합성 이메일 때문)
- **크론**: 매일 /api/cron/email-import (Gmail → OCR → 처방 자동저장)
